{
  "name": "known-universe-suite",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n COSMOSCOPE,ZERO \"npm:dev:cosmoscope\" \"npm:dev:zero\"",
    "dev:cosmoscope": "npm run dev -w @known-universe/cosmoscope -- --port 5173 --strictPort",
    "dev:zero": "npm run dev -w @known-universe/universe-zero -- --port 3001 --strictPort",
    "test": "vitest run",
    "check:packages": "tsc -p packages/core && tsc -p packages/engine && tsc -p packages/data && tsc -p packages/render-three",
    "check": "npm test && npm run check:packages && npm run lint --workspaces --if-present && npm run build --workspaces --if-present"
  }
}